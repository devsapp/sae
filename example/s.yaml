edition: 1.0.0        #  命令行YAML规范版本，遵循语义化版本（Semantic Versioning）规范
name: devsapp/sae   #  项目名称
services:
  start-sae:
    component: ${path(..)}
    props:
      region: cn-shenzhen
      appName: test5
      imageUrl: registry-vpc.cn-shenzhen.aliyuncs.com/sae-demo-image/consumer:1.0

     
